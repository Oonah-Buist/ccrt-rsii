This folder hosts client-side scripts.

Contact form
- contact-config.js: configure the JotForm embed (script/iframe/URL/ID)
- contact-embed.js: renders the embed into #contactFormEmbed
- contact-form.js: legacy (replaced by JotForm embed). You can delete it.

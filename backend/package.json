{
  "name": "ccrt-rsii-backend",
  "version": "1.0.0",
  "description": "Backend for CCRT RSII portal (admin, participant, BAA)",
  "main": "index.js",
  "scripts": {
    "start": "npm run rebuild:native && node index.js",
    "start:prod": "npm run rebuild:native && NODE_ENV=production node index.js",
    "dev": "nodemon index.js",
    "rebuild:native": "npm rebuild sqlite3",
    "build:frontend": "[ -d web ] && cd web && npm ci && npm run build || echo 'no frontend to build'"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "compression": "^1.7.4",
    "connect-sqlite3": "^0.9.15",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.2",
    "express-rate-limit": "^7.4.0",
    "express-session": "^1.17.3",
    "helmet": "^7.1.0",
    "morgan": "^1.10.0",
    "sqlite3": "^5.1.6"
  },
  "devDependencies": {
    "nodemon": "^3.0.3"
  },
  "engines": {
    "node": "20.x"
  }
}
